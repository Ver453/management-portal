@model first.Models.Faculty;

@{
    ViewData["Title"] = "Create";
    //Layout = "~/Views/Faculty/Create.cshtml";
    Layout = "_Layout";
}


<h4>facultyModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validar="faction-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-foulty_name" class="control-label"></label>
                <input asp-for="faculty_name" class="form-control" />
                <span asp-validation-for="faculty_name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Student" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
